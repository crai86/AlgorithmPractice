package com.patternmatching;

public class FiniteMachine {

	
	public void createStateMachine(String pattern){
		//pattern =a*b
		int m=pattern.length();
		// one more state than number of pattern length
		/**
		 * 
		 */
		int[][]t=new int[m+1][m];
		int lps=0;
		
		
		
	}
}
